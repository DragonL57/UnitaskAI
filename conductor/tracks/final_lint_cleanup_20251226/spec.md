# Specification: Final Linting Cleanup

## Overview
The codebase currently has linting errors related to `any` types, TS comments, and unused variables. We need to fix these to pass the quality gate.

## Tasks
1.  **Main Agent:** Define proper types for `messages` array and fix TS comments.
2.  **Memory Agent:** Remove unused variables.
3.  **Researcher & Scheduler Agents:** Fix TS comments for tools.
